need a db2 first

https://hub.docker.com/r/ibmcom/db2express-c/

docker run -it -p 50000:50000 -e DB2INST1_PASSWORD=Perficient101 -e LICENSE=accept ibmcom/db2express-c:latest bash
as root install git
$  yum install git

$ su - db2inst1
$ db2start
$ db2sampl

git clone this repo to get all the dbcreate scripts
